# FPS overlay — removed

**Status (2026-07-16): removed from OnionHEN.**

- No `fps_elf` target or `fps_elf.elf` embed in `daemon.elf`
- No game-process inject (`cmd_enable_fps*` deleted)
- No `fps.prx` asset in bootstrapper
- ShellUI monitor bar keeps CPU / GPU / RAM / IP only

Historical freeze notes about the GNM-WL detour are obsolete and were deleted
with the inject path. Do not re-enable soft-inject into titles without a new
design review.
